import { Given, When, Then } from '@cucumber/cucumber'

Given('attestations {}, {}, {}, {}, {}, {}, {}, {}, {}, {}', (score1, score2, score3, score4, score5, score6, score7, score8, score9, score10 ) => {
    return 'pending'
})

Given('the minimum number of attestations to make scoring relevant is {int}', (int) => {
    return 'pending'
})

When('I query the system with the email contact info {}', (email) => {
    return 'pending';
})